// src/app/api/lessons/route.ts

// All API route handlers (e.g., DELETE) previously in this file have been removed
// as the functionality is now handled by Server Actions.
// This file can be used if other HTTP methods (GET, POST, PUT) are needed for this specific route /api/courses/[id]
// in the future.

export {}; // Ensures the file is treated as a module if no other exports exist
